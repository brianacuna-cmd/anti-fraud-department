# Internal docs — never commit
docs/

# Node / TypeScript
node_modules/
dist/
coverage/
.cache/
.env
.env.*
*.log

# Local tool caches (machine-specific absolute paths)
.atl/

# SDD planning artifacts live in Engram only, not in the repo
openspec/

# Almacen local de evidencias (adaptador de filesystem). Son ficheros de
# prueba subidos a expedientes: no se versionan.
.evidence/
